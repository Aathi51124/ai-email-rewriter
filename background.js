chrome.runtime.onInstalled.addListener(() => {
  console.log("AI Email Rewriter installed.");
});
// Nothing needed for now. This file is reserved if we want to inject anything automatically later.
